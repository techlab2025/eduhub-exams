
services:
  eduhub-exams:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eduhub-exams
    ports:
      - "8080:80"
    restart: unless-stopped

# http://192.168.0.148:8080/choose-country